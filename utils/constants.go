package utils

const (
	// EnableImdsOnlyMode is used to specify that EC2 API calls should be skipped and only IMDS metadata should be
	// used to get ENI information.
	EnvEnableImdsOnlyMode = "ENABLE_IMDS_ONLY_MODE"
)
